server.servlet.context-path=/api
spring.application.name=table2taste-backend_springboot
spring.datasource.url=${DB_URL:jdbc:postgresql://db:5432/table2taste}
spring.datasource.username=${POSTGRES_USER:table-2-taste}
spring.datasource.password=${POSTGRES_PASSWORD:SCbK9nkVHcgjLS}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:false}

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect

# Hibernate ddl auto — desactivado, migraciones gestionadas por Liquibase
spring.jpa.hibernate.ddl-auto = none
spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.xml
spring.liquibase.enabled=true

app.jwt.secret=${JWT_SECRET:5ee9b307d37af03d6081d53f0ab8756b9d38b00f61a125525531e1b436b0f66d}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:3600000}

app.default.language=${DEFAULT_LANGUAGE:es}

app.default.filePath=${FILE_PATH:uploads/}

app.default.tablePassword=${TABLE_PASSWORD:xgEne4Kq}
springdoc.api-docs.path=/api-docs